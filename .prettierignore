# Build artifacts, generated by rollup and not tracked in git.
dist

# Claude Code nested checkouts; same reason as eslint.config.mjs ignores.
.claude/worktrees

# Prose is hand-formatted. Prettier reflows the JavaScript inside fenced code
# blocks, which destroys the aligned end-of-line comments in the README's
# examples, and it rewrites *emphasis* to _emphasis_ for no benefit.
# CHANGELOG.md is generated by conventional-changelog in any case.
*.md

package-lock.json
assets
